apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgvectordb-pvc
  namespace: db
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

